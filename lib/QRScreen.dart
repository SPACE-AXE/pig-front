import 'package:flutter/material.dart';

class QRScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("QR 스크린")),
      body: Center(child: Text("QR 스크린입니다")),
    );
  }
}
