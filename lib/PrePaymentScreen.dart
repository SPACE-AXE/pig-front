import 'package:flutter/material.dart';

class PrePaymentScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("사전결제 스크린")),
      body: Center(child: Text("사전결제 스크린입니다")),
    );
  }
}
